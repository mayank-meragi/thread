# Copy to .dev.vars for local `npm run rss-worker:dev`.
# Never commit the real values.
RSS_PROXY_KEY="replace-with-a-random-64-character-key"
ALLOWED_ORIGINS="http://127.0.0.1:5173,http://localhost:5173"
